// Landing spot for /portal/onboarding itself - the layout's gate redirects
// to the first incomplete step (or /complete) as soon as the account loads.
export default function OnboardingIndexPage() {
  return null;
}
